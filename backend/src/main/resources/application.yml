server:
  port: 8080

llm:
  provider: ${LLM_PROVIDER:claude}

anthropic:
  api:
    key: ${ANTHROPIC_API_KEY:}

openai:
  api:
    key: ${OPENAI_API_KEY:}

ollama:
  base:
    url: ${OLLAMA_BASE_URL:http://localhost:11434}

spring:
  application:
    name: resolve-pal